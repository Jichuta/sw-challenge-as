# Git commands

```bash
git init
  git remote add origin git@github-work:Jichuta/sw-challenge-as.git
  git status
  git add .
  git commit -m "Initial commit"
  git branch -M main
  git push -u origin main
```